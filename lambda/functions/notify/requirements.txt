# AWS SDK for Python (included in Lambda runtime, listed for local development)
boto3>=1.34.0
botocore>=1.34.0

# No additional external dependencies required
# The notify Lambda uses only Python standard library (urllib, json) and boto3
